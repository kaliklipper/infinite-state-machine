database:
  rdbms: mysql
  db_name: ism
  host: localhost
  user: state_admin

logging:
  file: ism.log
  level: debug
  propagate: true

runtime:
  root_dir: /tmp/ism
  stamp_format: epoch_milliseconds
